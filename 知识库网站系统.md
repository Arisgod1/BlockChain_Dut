# 小组知识库网站系统

## 总体方案

构建一个持续活跃的公开知识库：首页展示最近知识、技术方向和小组动态，招新作为辅助入口；全部内容静态生成并部署到 GitHub Pages，无运行时后端。

采用四层结构：

```text
knowledge/               唯一事实源，成员通过 PR 维护
site/src/content/generated/ Skill 生成的规范化网站内容；内容更新唯一可写的网站目录
site/src/                Astro 展示层
generated/manifest.json  Git、PR、来源文档与生成页面的溯源状态
packages/site-maintainer/ 确定性 TypeScript CLI
.agents/skills/site-maintainer/ 仓库内维护 Skill
```

技术栈固定为 Astro、TypeScript、Astro Content Collections、Pagefind、pnpm、GitHub Pages。页面默认零客户端 JS；只有搜索、移动导航和筛选器使用按需加载的 JS island。

## 内容系统与公开接口

- `knowledge/` 按 `group`、`tracks`、`meetings`、`projects`、`members`、`recruitment` 分类；从现在开始只接受统一 Markdown 格式。
- 通用 frontmatter 固定包含 `schemaVersion`、`id`、`title`、`summary`、`type`、`status`、`authors`、`tags`、`updatedAt`、`cover`、`media` 和 `references`。每条相关资料必须包含 `kind`、`title`、`url` 和 `source`。非例会内容另外包含 `publishedAt`；例会只使用 `heldAt` 和 `speakers`。Schema 为严格模式，未知字段和已经移除的旧字段直接阻断。当前 `schemaVersion` 为 `1`；`draft` 不公开，`published` 公开，`archived` 仍公开但必须显示归档标记；不设计登录区。
- 例会正文只包含内容；包含可链接到成员的主讲人和准确例会时间，相关资料统一由 `references` 渲染。
- 技术方向固定为内容、作者；通过开放 `tags` 分类，相关项目、例会和文章统一由 `references` 表达。
- ID 和 URL slug 发布后不可复用；重命名生成静态跳转，删除生成 tombstone 记录并从站点移除。
- `generated/manifest.json` 为机器接口。顶层记录 `schemaVersion`、`generatorVersion`、`processedSourceCommit`、`siteBuildContractVersion`、`entries`、`redirects` 和 `tombstones`；每项记录内容 ID、类型、状态、源路径、源 blob SHA、来源提交与 PR、生成路径、公开 URL、内容哈希、图片哈希和删除状态。时间戳不参与确定性比较。
- 维护 Skill 提供以下命令：
  - `site-maintainer update`：处理上次发布提交到当前 HEAD 的全部增量。
  - `site-maintainer rebuild --pr <number>`：按 PR 局部回源重建。
  - `site-maintainer check`：只校验，不修改。
  - `site-maintainer preview`：启动本地预览并输出检查地址。
  - `site-maintainer publish`：推送已检查的 `release/*` 分支并创建指向 `main` 的草稿 PR，不部署。

## Skill 状态机与 Git 流程

- 常规流程：人工 `git pull --ff-only` 后运行 `update`；Skill 从 manifest 读取 `processedSourceCommit`，使用 `git diff --name-status` 识别新增、修改、重命名和删除。
- Skill 创建 `release/<date>-<source-sha>` 分支；新增和修改重新提取，重命名保留跳转，删除移除页面并写入 tombstone。
- Git diff 决定实际变更范围；GitHub PR API 只补充 PR 编号、标题和 merge commit 等溯源信息，不以 PR 描述作为事实源。
- 大模型只在 `generated/suggestions/` 产生摘要、关联、导航文案和内容结构建议稿；人工确认并写回 `knowledge/` 后才能发布。标题、日期、作者、链接、状态等事实字段必须直接来自文档，禁止推断。
- CLI 在忽略的临时目录连续生成两份候选并比较规范化哈希；随后运行 Schema、敏感信息、内部链接、图片、构建和全文索引检查。全部通过后才原子替换生成目录和 Manifest，任何失败都恢复旧生成层且不推进 `processedSourceCommit`。
- 检查通过后生成 `generated/update-report.md`，包含来源提交、PR、增删改页面、删除记录、警告、构建指标和截图。
- `rebuild --pr` 通过 GitHub API取得该 PR 的知识文件，与 manifest 求交集，只替换对应生成内容；若文件已被后续 PR 修改则阻断并要求改为重建最新 PR，避免覆盖新知识。
- 普通内容 PR 只检查格式、引用、图片和敏感信息；合入 `main` 不生成也不部署。你检查本地 Preview 和 diff 后运行 `publish`，Release PR 完成全量回归并合入 `main`。
- 上线只能手动运行 Deploy Production 工作流。工作流部署成功后才快进 `production` 并创建不可变部署 Tag；`main` 可以包含尚未上线的事实源。
- 内容更新写入白名单固定为 `site/src/content/generated/`、`site/public/generated/`、`generated/manifest.json`、`redirects.json`、`tombstones.json`、`update-report.md` 和 `suggestions/`。组件、布局、页面、样式和 Astro 配置只能在独立人工开发任务中修改。
- `publish` 前重新确认当前 HEAD、Manifest 基线、工作区和全套验证；基线变化必须重新运行 `update`，不得直接 rebase 后发布。
- 删除前检查全部入站链接与图片引用；存在引用时阻断。旧 URL、redirect 和 tombstone 永久保留且不得重新分配。
- GitHub Pages 手动部署使用并发锁；部署失败不移动 `production`，也不替换上一成功产物。
- 正式发布后若升级 Schema，必须提供确定性迁移脚本；当前初期开发阶段不兼容已移除字段。未知版本直接阻断，模板、示例、CLI 与 Astro 使用同一 Schema 定义。

## 网站结构与视觉规范

- 导航固定为：首页、技术指导、例会、项目、成员、组内动态；全局提供搜索入口。
- 首页顺序固定为：最近更新、内容、最新例会、项目、小组介绍。
- 技术指导通过开放标签浏览；例会按 `heldAt` 年份筛选；成员按角色标签和入学时间筛选。Pagefind 负责全文搜索，搜索索引只在用户打开搜索时加载。
- 正式名称为“区块链组”。视觉严格参考 `downtime` 当前最终显示的浅色技术主题，而不是其早期暖色主题或中期深色终端主题。技术感来自信息结构、硬边框、等宽元数据和精确状态反馈；组名不得引导出链条、币、Web3、交易终端、霓虹或紫色科技模板。
- 全局背景以极浅灰绿为底，顶部允许非常轻的蓝色与琥珀色光晕；禁止深色主背景、装饰性网格和高饱和霓虹色。桌面内容容器最大宽度为 1120px，文章正文列为 680–760px；移动端左右留白 16–20px。
- 色彩全部以 OKLCH token 管理，并以参考项目最终主题为基准：背景 `oklch(0.955 0.006 160)`、表面 `oklch(0.985 0.002 160)`、正文 `oklch(0.18 0.025 170)`、次级文字 `oklch(0.43 0.026 170)`、强边框 `oklch(0.32 0.032 170)`、蓝色强调 `oklch(0.48 0.16 230)`、琥珀提醒 `oklch(0.66 0.16 58)`、危险色 `oklch(0.55 0.16 28)`。状态不能只依赖颜色表达，必须同时提供文字或图标。
- 顶部导航采用浅色实面工具栏：桌面端吸顶横向导航，移动端折叠为菜单与搜索按钮；不照搬参考项目的固定底部四栏导航。当前栏目使用浅蓝底、蓝色边框和明确文字状态。
- 首页首屏不是营销式 Hero，而是“本周研究台”：以紧凑标题、更新时间、三项以内的研究状态摘要和一个主要入口组成；下方直接露出最近更新。可使用真实项目截图、白板、活动和设备照片，但不使用抽象插画或模板化大卡片墙。
- 信息面板使用近白实色表面、1px 深浅分级边框和最多 1px 的短阴影；圆角统一为 4px。导航和搜索浮层也不默认使用毛玻璃，避免层层嵌套卡片。
- 列表采用“研究票据”式行项目：标题与摘要为主体，类型、日期和主讲人作为紧凑元数据；技术指导、例会、项目和组内动态使用与背景明确区分的内容面板。标签为小型胶囊，仅承担筛选与状态表达，不作为装饰。
- 成员使用横向自适应小卡片；按组长、老师、成员和入学时间排序，悬停或聚焦翻转显示个人简介，点击进入详情。
- 按钮按语义区分：主要操作使用深色实底，次要操作使用浅蓝底和蓝色边框，图标操作使用至少 44px 方形点击区；文字筛选项可以紧凑，但内容卡片和容器不得使用胶囊形或大圆角。
- 搜索页首屏直接显示输入框和状态反馈，首次搜索时按需加载 Pagefind；桌面端输入区最大宽度 720px，移动端单列。输入框、筛选器、空结果和加载状态均使用浅灰白实色表面与清晰描边。
- 中文正文使用系统无衬线字体栈，避免额外下载大型中文字体；机器字段、日期、状态和路径使用系统等宽字体。正文 16px、行高约 1.7，列表与元数据为 12–14px，文章标题不超过 44px。字距统一为 0，正文行长不超过 72ch。
- 交互动效使用 160–220ms 的快速缓出，只用于页面切换、筛选反馈、浮层开合和列表状态变化。必须支持 `prefers-reduced-motion`，关闭位移、缩放和连续动画，且动态内容不能引发布局跳动。
- 图片文件名使用小写英文连字符；封面统一 3:2、头像 1:1；正文图最大宽度 1600px，生成 AVIF/WebP 响应式尺寸，单张默认不超过 250KB。
- 每张图片必须提供 alt、来源和版权状态；构建时移除 EXIF。缺少 alt、尺寸异常、来源不明或路径重复均阻断发布。
- 品牌名称固定为“区块链组”。首版使用现有招牌、招新图标和合影：招牌放在页头身份区与组内动态；招新图标只放在组内动态的加入入口；合影放在首页小组介绍和组内动态。招牌发布图在生成阶段移除边缘连通白底。
- 详细视觉 token、组件和素材位置以根目录 `DESIGN.md` 为准。

## 知识文档与图片贡献规则

- `knowledge/` 是唯一事实源，内容目录固定为 `group`、`tracks`、`meetings`、`projects`、`members`、`recruitment`；`_templates` 和 `examples` 不参与发布。
- 所有 Markdown 使用小写英文连字符文件名。通用 Frontmatter 必须包含 `schemaVersion`、`id`、`title`、`summary`、`type`、`status`、`authors`、`tags`、`updatedAt`、`cover`、`media` 和 `references`。非例会类型增加 `publishedAt`；例会增加 `speakers` 和 `heldAt`，不得出现 `publishedAt`。
- `media` 对正文每张图片登记 `path`、`alt`、`source`、`rights`，可选 `caption` 和 `creator`。事实字段不得由大模型推断。
- 技术方向正文固定为内容、作者；例会正文只包含内容。成员必须增加两位数 `grade` 和结构化 `contacts`；全部资料关系使用结构化 `references`。
- 图片按用途和归属放入 `knowledge/assets/site/{brand,photos}`、`covers/<type>/<entry-id>`、`inline/<type>/<entry-id>`、`avatars/<member-id>` 或 `events/<yyyy>/<event-id>`；禁止把新图片放在仓库根目录或生成目录。普通源图单张不超过 8MB，完整相册和工程文件保存在外部归档。
- 详细写法见 `docs/content-authoring.md`，图片尺寸、命名、版权和新增流程见 `docs/image-guidelines.md`；可复制模板与中性示例位于 `knowledge/_templates/` 和 `knowledge/examples/`。
- 示例文档只演示结构，不编写或虚构区块链相关知识。所有示例保持 `status: draft`，不会进入公开站点。

## 验证与验收

- 单元测试覆盖 Markdown Schema、Git diff 映射、重命名、删除、manifest 更新、PR 局部重建和后续 PR 冲突保护。
- 集成测试使用包含新增、修改、重命名、删除的临时 Git 历史，验证增量结果与全量重建完全一致。
- 构建测试覆盖草稿不发布、归档标记、旧 URL 跳转、404、站点地图、RSS、Pagefind 中文搜索和 GitHub Pages 子路径。
- Playwright 检查桌面与移动端首页、目录、文章、搜索、空结果、超长标题和图片缺失场景，无重叠、溢出或布局跳动。
- 页面契约固定导航入口、首页区块顺序、每类代表页面、历史 URL、404、空结果、搜索交互、Sitemap、RSS 和 canonical URL；未变更页面输出哈希必须保持不变。视觉测试固定字体、时区、内容顺序并关闭动画。
- 发布门槛：Lighthouse Performance、Accessibility、Best Practices、SEO 均不低于 95；初始压缩 JS 不超过 50KB，CSS 不超过 35KB，移动端 LCP 目标不超过 2.5 秒。
- 敏感信息检查覆盖邮箱白名单之外的联系方式、密钥模式、内网 URL、私人姓名字段和图片 EXIF；任何命中都需要人工确认或修复。
- GitHub Pages 保留上一成功部署产物；部署失败不替换线上版本，回滚通过恢复上一网站更新提交完成。

## 已确定假设

- 同一仓库保存知识源、网站、Skill 和生成结果。
- 网站全部公开，不包含账号、权限、评论、在线编辑或语义检索。
- 内容作者从现在开始只使用新模板；严格 Schema 不兼容任何已移除字段。
- 更新由你在本地手动启动带大模型的 Skill，不由 CI 自动提取内容。
- 生成结果进入独立更新分支，人工检查并合入后才部署。
- 第一版以知识查阅为主，招新入口仅提供交流群和邮件。

# 小组知识库网站系统

## 总体方案

构建一个持续活跃的公开知识库：首页展示最近知识、技术方向和小组动态，招新作为辅助入口；全部内容静态生成并部署到 GitHub Pages，无运行时后端。

采用四层结构：

```text
knowledge/               唯一事实源，成员通过 PR 维护
site/src/content/        Skill 生成的规范化网站内容
site/src/                Astro 展示层
generated/manifest.json  Git、PR、来源文档与生成页面的溯源状态
```

技术栈固定为 Astro、TypeScript、Astro Content Collections、Pagefind、pnpm、GitHub Pages。页面默认零客户端 JS；只有搜索、移动导航和筛选器使用按需加载的 JS island。

## 内容系统与公开接口

- `knowledge/` 按 `group`、`tracks`、`meetings`、`projects`、`members`、`recruitment` 分类；从现在开始只接受统一 Markdown 格式。
- 通用 frontmatter 固定包含 `id`、`title`、`summary`、`type`、`status`、`authors`、`tags`、`publishedAt`、`updatedAt`、`cover`。`status` 为 `draft | published | archived`；除草稿外全部公开，不设计登录区。
- 例会正文固定为标题、内容、相关资料；包含主讲人、时间。
- 技术方向固定为方向介绍、知识地图、推荐路径、实践项目、相关例会、维护人。
- ID 和 URL slug 发布后不可复用；重命名生成静态跳转，删除生成 tombstone 记录并从站点移除。
- `generated/manifest.json` 为机器接口，逐项记录内容 ID、源路径、源 blob SHA、来源 PR、生成路径、公开 URL、生成时间、生成器版本和删除状态。
- 维护 Skill 提供以下命令：
  - `site-maintainer update`：处理上次发布提交到当前 HEAD 的全部增量。
  - `site-maintainer rebuild --pr <number>`：按 PR 局部回源重建。
  - `site-maintainer check`：只校验，不修改。
  - `site-maintainer preview`：启动本地预览并输出检查地址。
  - `site-maintainer publish`：推送已检查的网站更新分支并创建 PR。

## Skill 状态机与 Git 流程

- 常规流程：人工 `git pull --ff-only` 后运行 `update`；Skill 从 manifest 读取 `lastSourceCommit`，使用 `git diff --name-status` 识别新增、修改、重命名和删除。
- Skill 创建 `site/update-<date>-<short-sha>` 分支；新增和修改重新提取，重命名保留跳转，删除移除页面并写入 tombstone。
- Git diff 决定实际变更范围；GitHub PR API 只补充 PR 编号、标题和 merge commit 等溯源信息，不以 PR 描述作为事实源。
- 大模型只负责摘要、关联、导航文案和知识地图整理；标题、日期、作者、链接、状态等事实字段必须直接来自文档，禁止推断。
- 生成后依次运行 Schema、敏感信息、内部链接、图片、构建、全文索引、可访问性和视觉回归检查；硬错误阻止提交与部署。
- 检查通过后生成 `generated/update-report.md`，包含来源提交、PR、增删改页面、删除记录、警告、构建指标和截图。
- `rebuild --pr` 通过 GitHub API取得该 PR 的知识文件，与 manifest 求交集，只替换对应生成内容；若文件已被后续 PR 修改则阻断并要求改为重建最新 PR，避免覆盖新知识。
- 你检查本地 Preview 和 diff 后运行 `publish`；网站更新 PR 合入 `main` 后，GitHub Actions 构建并部署 GitHub Pages。内容 PR 合入本身不直接发布网站。

## 网站结构与视觉规范

- 导航固定为：首页、技术方向、项目与成果、例会、成员、关于与加入；全局提供搜索入口。
- 首页顺序固定为：当前研究状态、最近更新、技术地图、最新例会决策、精选成果、小组介绍、交流群与邮件。
- 技术地图负责跨方向浏览；Pagefind 负责全文搜索，并支持方向、内容类型、年份和标签筛选。搜索索引只在用户打开搜索时加载。
- 视觉参考 `down-time` 项目，定位为“温和的研究记录台”：克制、亲近、低压力，同时保持知识内容需要的清晰度和可信感。借用其暖色纸张背景、鼠尾草绿主色、半透明面板、票据式信息分组和轻量动效，不照搬游戏化文案、罗盘或移动端应用结构。
- 全局背景使用暖米色到极浅灰绿的柔和层次；禁止大面积纯白、深色科技风、紫色主视觉和高饱和霓虹色。桌面内容容器最大宽度为 1120px，文章正文列为 680–760px；移动端左右留白 16–20px。
- 色彩全部以 OKLCH token 管理，并以参考项目现有颜色为近似基准：纸张背景 `#f7f1e6`、浅背景 `#fbf7ef`、正文 `#2f3d38`、次级文字 `#6f7f78`、鼠尾草绿 `#8fa99b`、强调绿 `#6f8d80`、雾蓝 `#8fb0bd`、杏色提醒 `#e8bfa8`、危险色 `#b8645d`。状态不能只依赖颜色表达，必须同时提供文字或图标。
- 顶部导航采用轻量、半透明的纸面工具栏：桌面端吸顶横向导航，移动端折叠为菜单与搜索按钮；不照搬参考项目的固定底部四栏导航。当前栏目使用浅鼠尾草底色和明确文字状态，不使用粗重下划线。
- 首页首屏不是营销式 Hero，而是“本周研究台”：以紧凑标题、更新时间、三项以内的研究状态摘要和一个主要入口组成；下方直接露出最近更新。可使用真实项目截图、白板、活动和设备照片，但不使用抽象插画或模板化大卡片墙。
- 信息面板使用半透明暖白或浅灰绿表面、1px 低对比描边和柔和阴影；圆角统一为 8px。毛玻璃只用于导航、搜索浮层和少量重点面板，普通文章区块保持实色背景，避免层层嵌套卡片。
- 列表采用“研究票据”式行项目：标题与摘要为主体，类型、方向、日期、作者作为紧凑元数据；最近更新、例会决策和项目成果优先使用分隔线列表，仅对精选内容使用独立卡片。标签为小型胶囊，仅承担筛选与状态表达，不作为装饰。
- 技术地图使用清晰的路径、节点和分组关系，鼠尾草绿表示当前方向，雾蓝表示关联知识，杏色表示待补充内容；地图在移动端转为纵向路径列表，不能依赖横向拖动才能阅读。
- 按钮继承参考项目的柔和手感，但按语义区分形态：主要操作使用鼠尾草绿实底，次要操作使用暖白描边，图标操作使用 44px 方形点击区；文字按钮与筛选项可使用胶囊形，内容卡片和容器不得使用超大圆角。
- 搜索打开后使用居中的命令面板式浮层，首屏直接显示输入框、最近更新和常用筛选；桌面端最大宽度 720px，移动端接近全屏。输入框、筛选器、空结果和加载状态均沿用暖白表面与清晰描边。
- 中文正文使用 `Inter`、`Nunito Sans`、系统无衬线字体栈，避免额外下载大型中文字体；正文 16px、行高约 1.7，列表与元数据为 12–14px，文章标题不超过 44px。字距统一为 0，正文行长不超过 72ch。
- 交互动效使用 200–320ms 的柔和缓动，只用于页面切换、筛选反馈、浮层开合和列表进入；按压可缩放至 0.985。必须支持 `prefers-reduced-motion`，关闭位移、缩放和连续动画，且动态内容不能引发布局跳动。
- 图片文件名使用小写英文连字符；封面统一 3:2、头像 1:1；正文图最大宽度 1600px，生成 AVIF/WebP 响应式尺寸，单张默认不超过 250KB。
- 每张图片必须提供 alt、来源和版权状态；构建时移除 EXIF。缺少 alt、尺寸异常、来源不明或路径重复均阻断发布。
- 品牌名称、Logo、交流群、邮箱和实际图片先通过单一品牌配置留空，未来替换时不改页面结构。

## 验证与验收

- 单元测试覆盖 Markdown Schema、Git diff 映射、重命名、删除、manifest 更新、PR 局部重建和后续 PR 冲突保护。
- 集成测试使用包含新增、修改、重命名、删除的临时 Git 历史，验证增量结果与全量重建完全一致。
- 构建测试覆盖草稿不发布、归档标记、旧 URL 跳转、404、站点地图、RSS、Pagefind 中文搜索和 GitHub Pages 子路径。
- Playwright 检查桌面与移动端首页、目录、文章、搜索、空结果、超长标题和图片缺失场景，无重叠、溢出或布局跳动。
- 发布门槛：Lighthouse Performance、Accessibility、Best Practices、SEO 均不低于 95；初始压缩 JS 不超过 50KB，CSS 不超过 35KB，移动端 LCP 目标不超过 2.5 秒。
- 敏感信息检查覆盖邮箱白名单之外的联系方式、密钥模式、内网 URL、私人姓名字段和图片 EXIF；任何命中都需要人工确认或修复。
- GitHub Pages 保留上一成功部署产物；部署失败不替换线上版本，回滚通过恢复上一网站更新提交完成。

## 已确定假设

- 同一仓库保存知识源、网站、Skill 和生成结果。
- 网站全部公开，不包含账号、权限、评论、在线编辑或语义检索。
- 内容作者从现在开始遵守统一模板，不长期兼容旧格式。
- 更新由你在本地手动启动带大模型的 Skill，不由 CI 自动提取内容。
- 生成结果进入独立更新分支，人工检查并合入后才部署。
- 第一版以知识查阅为主，招新入口仅提供交流群和邮件。

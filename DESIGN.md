# 区块链组知识库视觉设计

## 设计方向

视觉严格参考 `/Users/arisone/projects/downtime` 当前最终显示的浅色技术主题，而不是其早期暖色主题或中期深色终端主题。整体应像一套真实运行的研究资料系统：浅色、精确、有纸面结构感，但不复刻终端界面。

组名只用于身份识别。禁止根据“区块链”一词加入链条、币、Web3、霓虹、交易图表或深色科技背景。

## 色彩

所有实现色使用 OKLCH token：

```css
:root {
  --color-bg: oklch(0.955 0.006 160);
  --color-bg-deep: oklch(0.905 0.012 165);
  --color-surface: oklch(0.985 0.002 160);
  --color-surface-muted: oklch(0.955 0.004 160);
  --color-text: oklch(0.18 0.025 170);
  --color-text-muted: oklch(0.43 0.026 170);
  --color-text-soft: oklch(0.31 0.032 170);
  --color-line: oklch(0.74 0.018 170);
  --color-line-strong: oklch(0.32 0.032 170);
  --color-accent: oklch(0.48 0.16 230);
  --color-accent-soft: oklch(0.91 0.055 230);
  --color-warning: oklch(0.66 0.16 58);
  --color-danger: oklch(0.55 0.16 28);
}
```

背景允许在页面顶端使用非常轻的蓝色和琥珀色径向光晕，但不得形成大面积渐变装饰。内容表面以实色为主，不使用装饰性毛玻璃。

## 字体与排版

- 中文正文使用系统无衬线字体栈；不下载大型中文字体。
- 拉丁字母优先使用系统 UI 字体，元数据和机器字段使用系统等宽字体。
- 正文 16px，行高 1.7，最大 72ch；元数据 12–14px。
- 页面标题最大 44px，字距为 0；标题使用高字重建立层级。
- 等宽字体只用于日期、类型、状态、路径和筛选标签，不用于长正文。

## 形状与层级

- 面板、输入框、按钮和独立内容块统一使用 4px 圆角。
- 胶囊仅用于标签、筛选和状态；文章和卡片禁止大圆角。
- 普通表面使用 1px 实线硬边框和最多 1px 的短阴影。
- 列表内部使用低对比实线或虚线分隔，避免把每一项都做成浮动卡片。
- 焦点状态使用蓝色描边和外扩焦点环；选中状态不能只改变颜色。

## 布局

- 桌面主容器最大宽度 1120px，正文列 680–760px。
- 移动端左右留白 16–20px，所有信息图转为单列阅读顺序。
- 顶部使用吸顶横向导航，完整包含首页、技术指导、例会、项目、成员和组内动态；桌面端用单一滑块在当前项与悬停项之间自然移动，移动端折叠菜单，不使用固定底部导航。
- 首页首屏为“本周研究台”，不使用营销式大 Hero。最近更新直接进入首屏后的阅读流。
- 真实合影适合放在“小组介绍”区块，使用 3:2 横向裁切；招牌用于页头或组内动态身份区；招新图标只用于组内动态的加入入口，不作为全站装饰。

## 组件

- 研究票据：标题和摘要为主体；类型、方向、日期、作者使用紧凑等宽元数据。
- 主要按钮：深色实底；次要按钮：蓝色浅底与蓝色边框；普通按钮：浅表面与深色边框。
- 搜索浮层：桌面最大 720px，移动端接近全屏；打开时才加载 Pagefind 索引。
- 内容筛选：技术指导使用开放标签；例会按准确日期；成员按角色标签和入学时间。筛选控件必须可键盘操作，并显示结果数量。
- 成员卡片：桌面与移动端使用自适应小卡片；悬停或聚焦翻转显示简介，减少动态模式不翻转；点击进入成员详情。
- 图片说明：图片下方显示说明文字；来源与版权信息进入可展开的元数据区域或文章末尾资料区。

## 动效

交互反馈使用 160–220ms 的快速缓出。只对导航、筛选、搜索浮层和列表状态变化使用动效。禁止连续背景动画。`prefers-reduced-motion: reduce` 下移除位移和缩放。

## 素材位置

- `knowledge/assets/site/brand/blockchain-group-sign.png`：站点招牌。
- `knowledge/assets/site/brand/recruitment-mark.png`：加入入口图标。
- `knowledge/assets/site/photos/group-photo-original.jpg`：合影源图，仅作事实源，不直接发布。
- 构建阶段从原图生成去除 EXIF、压缩后的 3:2 AVIF/WebP 响应式版本。

完整图片命名、元数据和发布规则见 `docs/image-guidelines.md`。
